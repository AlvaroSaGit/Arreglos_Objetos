
export default function cantidad_total_registrado(ADSO) {
    // La función recibe el arreglo ADSO y devuelve la cantidad total de nombres registrados.
    return ADSO.length; // .length es una propiedad 
    // que devuelve el número de elementos de un arreglo.
}