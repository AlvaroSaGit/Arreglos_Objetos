
export default function recorrer_usando_for(notas) {
    // ultilizamos un bucle for para recorrer el arreglo de notas y mostrar cada nota en la consola.
    for ( let i = 0; i < notas.length; i++) {
        console.log(notas[i]);
    
    }
}