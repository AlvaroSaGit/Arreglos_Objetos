

export default function recorre_usando_for_of(frutas) {
   for (let fruta of frutas) {
    console.log(fruta); // Imprime cada fruta en la consola utilizando un bucle for...of para recorrer el arreglo de frutas.
   }
}