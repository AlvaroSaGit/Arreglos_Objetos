
export default function retirar_ultimo_elemento(frutas) {
    frutas.pop(); // .pop() es un metodo que elimina el último elemento de un arreglo y devuelve ese elemento.
    return frutas; // Devuelve el arreglo modificado sin el último elemento.
}